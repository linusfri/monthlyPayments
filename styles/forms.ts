import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    base: {
        marginTop: 12
    },

    input: {
        fontSize: 20,
        marginBottom: 28,
        borderWidth: 1,
        padding: 10,
        borderColor: '#ccc',
        borderRadius: 3,
    },

    formButton: {
        paddingTop: 15,
        paddingBottom: 15,
        paddingRight: 10,
        paddingLeft: 10,
        backgroundColor: '#339966',
        borderRadius: 2,
        marginBottom: 12,
    },

    formFieldCenter: {
        textAlign: 'center'
    }
});

export {styles};
